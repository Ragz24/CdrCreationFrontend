import { PlanCategoryPipe } from './plan-category.pipe';

describe('PlanCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
